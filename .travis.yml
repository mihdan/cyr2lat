sudo: false

language: php

notifications:
  email:
    on_success: never
    on_failure: change

matrix:
  fast_finish: true
  include:
    - php: '5.4'
      env:
        - DEV="--no-dev"
    - php: '5.5'
      env:
        - DEV="--no-dev"
    - php: '5.6'
      env:
        - DEV="--no-dev"
    - php: '7.0'
      env:
        - DEV="--no-dev"
    - php: '7.1'
      env:
        - DEV="--no-dev"
    - php: '7.2'
      env:
        - DEV="--no-dev"
    - php: '7.2'
      env:
        - SNIFF=1 TESTS=1
    - php: '7.3'
      env:
        - DEV="--no-dev"
    - php: '7.3'
      env:
        - SNIFF=1 TESTS=1

cache:
  directories:
    - vendor
    - $HOME/.composer/cache
    - node_modules

before_script:
  - phpenv rehash; composer install $DEV

script:
  - if [[ "$SNIFF" == "1" ]]; then vendor/bin/phpcs -p -s --colors --standard=phpcs.xml .; fi
  - if [[ "$TESTS" == "1" ]]; then vendor/phpunit/phpunit/phpunit --configuration tests/phpunit/phpunit.xml; fi
